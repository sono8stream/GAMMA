service firebase.storage {
  match /b/{bucket}/o {
    match /blogs/{allPaths=**} {
      allow read, write;
    }
    match /{allPaths=**} {
      allow read, write: if request.auth!=null;
    }
  }
}
